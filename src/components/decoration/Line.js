/** @jsx jsx */
import React from "react";
import { css, jsx } from "@emotion/core";

export default () => (
  <div
    css={css`
      border-bottom: 1px solid darkgray;
      width: 50%;
      margin: auto;
    `}
  ></div>
);
